export default function ClubBookingsPage() {
  return (
    <main className="min-h-screen px-6 py-16">
      <div className="mx-auto flex w-full max-w-5xl flex-col gap-4">
        <p className="text-sm uppercase tracking-[0.35em] text-[var(--gold)]">Club bookings</p>
        <h1 className="text-4xl font-semibold tracking-tight">Prenotazioni venue</h1>
        <p className="max-w-2xl text-base leading-8 text-[var(--muted)]">
          Placeholder per lista prenotazioni con filtri per evento e stato.
        </p>
      </div>
    </main>
  );
}
