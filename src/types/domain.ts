export type GlobalRole = "super_admin";

export type VenueRole = "owner" | "manager" | "staff";

export type EventStatus = "draft" | "published" | "closed" | "archived";

export type BookingStatus = "pending" | "confirmed" | "rejected" | "cancelled";

export type NotificationChannel = "email" | "whatsapp" | "internal";

export type BottleKind = "included" | "extra";
